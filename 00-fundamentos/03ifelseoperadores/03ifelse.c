#include <stdio.h>

int main() {
    int numero;
    printf("Digite sua idade: \n");
    scanf("%d",&numero);
    if (numero >= 18) {
        printf("Maior de idade\n");
    } else {
        printf("Menor de idade\n");
    }
    return 0;

    // = atribuição
    // == igualdade
    // > maior
    // < menor
    // >= <= ''eigual
    // != direfente
    // && and
    // || or
}